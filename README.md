# JQuery-JavaScript-Referrer-Information-Cookie
JQuery - JavaScript Referrer Information Cookie

Dependencies:
https://freegeoip.net/ needs to be up and running.

```javascript

var tr = new rInfo();

tr.set(1);
tr.get("Location");
tr.getAll();

```
